{{ form_start(form) }}
{# {{ form_widget(form.save, { 'label': 'Envoyer' }) }} #}
{# <button class="btn btn-primary">{{ button_label|default('Envoyer') }}</button> #}
{{ form_row(form.picture) }}
{{ form_end(form) }}
