{% extends 'base.html.twig' %}

{% block title %}Noter le produit
	{# {{ product.name }} #}
{% endblock %}

{% block body %}
	<div class="d-flex flex-column align-items-center mt-4">
		<h1>Noter le produit
			{# {{ product.name }} #}
		</h1>
	</div>

	{{ include('review/_form.html.twig') }}

	<a href="{{ path('app_product') }}">Retour à la page produit</a>
{% endblock %}
